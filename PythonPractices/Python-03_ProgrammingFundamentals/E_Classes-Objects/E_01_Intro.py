'''
Python is an object-oriented programming (OOP) language that uses a paradigm centered around OBJECTS and CLASSES

CLASSES
    - is a blueprint or template for creating objects
    - defines structure and behavior that its objects will have
To create a class, we specify ATTRIBUTES (the data) and the METHOD (functions) that objects of that class will have
    - ATTRIBUTES: variables within the class
    - METHODS: functions

OBJECTS
    - fundamental unit in Python that represents a real world entity or concept
    - can be tangible or abstract
Every object has TWO MAIN CHARACTERISTICS:
1) STATE:       the attribute or data that describes the object
2) BEHAVIOR:    the actions or methods that the object can perform.
                methods are functions that belong to objects and can change the object's state or perform specific operations

example:
    CLASS:  Cars() [color,speed,actions]
    OBJECT: Car1=Car("red",45,accelerate)
        ATTRIBUTES / STATE:
            - Car1.color = "red"
            - Car1.speed = 45 [km/h]
        METHODS / BEHAVIOR:
            - accelerate()
            - slowdown()
        
            



'''